{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm start",
            "env": {"NODE_OPTIONS": "--openssl-legacy-provider"},
            "name": "Run npm start",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}